#!/bin/bash
#
# usage:
# npm install -g junc
# pushj="$(PUSHJ_PATH)"
# popj="$(POPJ_PATH)"
#
# use similiar to pushd / popd to move to a temporary junctioned directory
# . "$pushj"
# . "$popj"

pushd "$(junc)"
